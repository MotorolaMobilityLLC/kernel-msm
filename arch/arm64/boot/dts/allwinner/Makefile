# SPDX-License-Identifier: GPL-2.0
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-amarula-relic.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-bananapi-m64.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-nanopi-a64.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-oceanic-5205-5inmfd.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-olinuxino.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-olinuxino-emmc.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-orangepi-win.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-pine64-lts.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-pine64-plus.dts sun50i-a64-pine64.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-pinebook.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-sopine-baseboard.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-a64-teres-i.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h5-bananapi-m2-plus.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h5-bananapi-m2-plus-v1.2.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h5-emlid-neutis-n5-devboard.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h5-libretech-all-h3-cc.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h5-nanopi-neo2.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h5-nanopi-neo-plus2.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h5-orangepi-pc2.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h5-orangepi-prime.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h5-orangepi-zero-plus.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h5-orangepi-zero-plus2.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h6-beelink-gs1.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h6-orangepi-3.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h6-orangepi-lite2.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h6-orangepi-one-plus.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h6-pine-h64.dts
dts-$(CONFIG_ARCH_SUNXI) += sun50i-h6-tanix-tx6.dts
